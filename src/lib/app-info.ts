// Fill this in once the app is actually published somewhere a recipient can
// install it from (App Store / Play Store / TestFlight / an EAS Update
// link) - e.g. 'https://apps.apple.com/app/id...'. Left null until then:
// the shared message (see src/app/event/[id].tsx) still adds the branded
// "Sent with Deiva Dinam" line either way, it just skips the install
// line rather than link to somewhere that doesn't exist yet.
export const APP_INSTALL_URL: string | null = null;
